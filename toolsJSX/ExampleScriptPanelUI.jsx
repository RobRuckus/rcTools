{    function myScript(thisObj){        function myScript_buildUI(thisObj){            var myPanel=(thisObj instanceof Panel) ? thisObj : new Window("palette","MywindowName",undefined,{resizable:true});            res = "group{orientation:'row',\                        groupOne: Group{orientation:'column',\                        myButton: Button{text:'Visible Name'},\                        myCheckBox:Checkbox{text: 'CheckBox'},\                        myDropDownList: DropDownList{properties:{items:['DropDown','DropDown2','DropDown3']}},\                     },\            }";            myPanel.grp=myPanel.add(res)            return myPanel;                   }        var myScriptPal=myScript_buildUI (thisObj)        if((myScriptPal != null) && (myScriptPal instanceof Window)){//Window Create if not Panel                myScriptPal.center();                myScriptPal.show();             }        }myScript(this);  }