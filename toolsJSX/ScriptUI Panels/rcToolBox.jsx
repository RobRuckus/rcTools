{    function myScript(thisObj){        function myScript_buildUI(thisObj){            var myPanel=(thisObj instanceof Panel) ? thisObj : new Window("palette","rcTools",undefined,{resizable:true});            res = "group{}";            myPanel.grp=myPanel.add(res)            //DEFAULTS                        return myPanel;                   }        var myScriptPal=myScript_buildUI(thisObj)        if((myScriptPal != null) && (myScriptPal instanceof Window)){//Window Create if not Panel                myScriptPal.center();                myScriptPal.show();             }        }myScript(this);  }function cleanUP(){    alert("WORKS");    }